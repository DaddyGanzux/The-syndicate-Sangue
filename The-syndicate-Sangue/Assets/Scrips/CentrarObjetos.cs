using UnityEngine;

public class CentrarObjetos : MonoBehaviour
{

   /* public void Centrar()
    {
        if (Physics.Raycast(transform.position, Vector3.down, out RaycastHit hitInfo1));
        {
            transform.position = new Vector3(hitInfo1.point.x, transform.position.y, hitInfo1.point.z);
        }
    }
   */
}
